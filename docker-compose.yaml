services:
  app:
    build: .
    ports:
      - "5002:5002"
    volumes:
      - ./data:/app/data  # SQLite DB + backups persist here
      - ./.env:/app/.env
    environment:
      - FLASK_ENV=production
